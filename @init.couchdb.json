{
  "@storage": {
    "bootstrap": {
      "id": "bootstrap",
      "class_id": "@storage",
      "type": "couchdb",
      "server": "http://elementstore_couchdb:5984",
      "_note": "username/password: override via COUCHDB_USER and COUCHDB_PASSWORD env vars (see ClassModel::boot)"
    }
  }
}
