.idea/
keys/
vendor/
node_modules/
.env
.env.local

# @init.json is environment-specific and may contain secrets.
# Copy @init.json.example to @init.json and set COUCHDB_USER / COUCHDB_PASSWORD env vars.
@init.json

# es-client build outputs
packages/es-client/dist/
admin/dist/

# .es/ directory: track genesis/seed files, ignore runtime data
.es/*.json
!.es/*.genesis.json
!.es/*.seed.json
.es/*/
env_override.php
